name: Build Big Brain AI APK
"on":
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build APK via Kivy Docker
        run: |
          mkdir -p $HOME/.buildozer
          docker run --rm \
            -v ${{ github.workspace }}:/home/user/hostcwd \
            -v $HOME/.buildozer:/home/user/.buildozer \
            -e ANDROIDSDK=/home/user/.buildozer/android/platform/android-sdk \
            -e ANDROIDNDK=/home/user/.buildozer/android/platform/android-ndk-r25c \
            -e ANDROIDAPI=33 \
            -e ANDROIDMINAPI=26 \
            kivy/buildozer:latest \
            android debug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: BigBrainAI
          path: bin/*.apk
          retention-days: 30
